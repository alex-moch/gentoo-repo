--- a/src/cpp/cli/CMakeLists.txt
+++ b/src/cpp/cli/CMakeLists.txt
@@ -13,7 +13,10 @@
 if(NOT TARGET lemonade-digest-crypto)
     find_package(PkgConfig QUIET)
     if(PkgConfig_FOUND)
-        pkg_check_modules(MBEDCRYPTO QUIET mbedcrypto)
+        # Gentoo's net-libs/mbedtls names its pkg-config module (and its
+        # library/header install paths) with the ABI slot suffix
+        # (mbedcrypto-3), not plain mbedcrypto; try both.
+        pkg_search_module(MBEDCRYPTO QUIET mbedcrypto mbedcrypto-3)
     endif()

     add_library(lemonade-digest-crypto INTERFACE)

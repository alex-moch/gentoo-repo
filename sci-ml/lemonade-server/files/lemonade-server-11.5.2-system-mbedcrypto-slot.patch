--- a/CMakeLists.txt
+++ b/CMakeLists.txt
@@ -396,6 +396,11 @@
     find_package(PkgConfig QUIET)
     if(PkgConfig_FOUND)
         pkg_check_modules(MBEDTLS QUIET mbedtls mbedx509 mbedcrypto)
+        if(NOT MBEDTLS_FOUND)
+            # Gentoo's net-libs/mbedtls is SLOT 3 and suffixes its pkg-config
+            # modules with the ABI slot (mbedtls-3, mbedx509-3, mbedcrypto-3).
+            pkg_check_modules(MBEDTLS QUIET mbedtls-3 mbedx509-3 mbedcrypto-3)
+        endif()
     endif()

     add_library(lemonade-digest-crypto INTERFACE)
